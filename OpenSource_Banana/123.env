# ------------------
#  服务器运行环境
# ------------------
NODE_ENV=production

# ------------------
#  后端服务端口
# ------------------
PORT=3006

# ------------------
#  数据库连接配置
# ------------------
# 请修改为你自己的数据库信息
DB_HOST=127.0.0.1
DB_USER=your_db_username
DB_PASSWORD=your_db_password
DB_NAME=your_db_name

# ------------------
#  用户认证 (JWT)
# ------------------
# 🔐 务必修改为复杂的随机字符串！
JWT_SECRET=change_this_to_a_long_random_string

# ------------------
#  AI 模型 API
# ------------------
# 如果使用中转，请填入中转地址；如果使用官方，填 https://api.openai.com
AI_API_BASE_URL=https://api.jungpt.com
# 🔴 也就是 sk- 开头的密钥，千万不要直接提交到代码里！
AI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ------------------
#  CORS 跨域配置
# ------------------
# 生产环境建议改为具体的域名，而不是 *
FRONTEND_URL=http://localhost:3000

# === 邮箱配置 (SMTP) ===
# 这里以 QQ 邮箱为例
MAIL_HOST=smtp.qq.com
MAIL_PORT=465
MAIL_USER=your_email@qq.com
# 🔴 这里填写邮箱的 SMTP 授权码，不是 QQ 登录密码
MAIL_PASS=your_email_auth_code